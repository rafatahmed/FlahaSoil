{
	"name": "@flaha/soil-chemistry",
	"version": "1.0.0",
	"private": true,
	"description": "Soil chemistry engine — CEC, base saturation, cation balance, ESP, SAR. Calculation only; no interpretation.",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"build": "echo \"[packages/soil-chemistry] build placeholder — wired in Phase 3\"",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^1.6.1"
	}
}
