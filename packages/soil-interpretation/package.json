{
	"name": "@flaha/soil-interpretation",
	"version": "1.0.0",
	"private": true,
	"description": "Pure decision-layer engine that classifies physics + chemistry results into qualitative agronomic categories, risk classes, and warnings.",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"build": "tsc -p tsconfig.json",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^1.6.1"
	}
}
