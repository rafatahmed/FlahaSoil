{
	"name": "@flaha/shared-types",
	"version": "1.0.0",
	"private": true,
	"description": "Shared TypeScript DTOs, enums, API contracts, and error envelope for FlahaSOIL v2 frontend and backend.",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "echo \"[packages/shared-types] no runtime tests — types-only package\""
	}
}
